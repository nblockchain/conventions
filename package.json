{
    "type": "module",
    "dependencies": {
        "cosmiconfig": "9.0.0"
    },
    "devDependencies": {
        "@types/node": "^25.2.0",
        "vitest": "^1.3.1"
    },
    "scripts": {
        "format": "npx prettier --version && npx prettier --quote-props=consistent --write './**/*.ts' && npx prettier --quote-props=consistent --write './**/*.yml'",
        "test": "vitest --globals=true"
    }
}
