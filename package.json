{
    "type": "module",
    "dependencies": {
        "cosmiconfig": "9.0.0"
    },
    "devDependencies": {
        "prettier": "2.8.3",
        "@types/node": "^25.2.0",
        "vitest": "^1.3.1"
    },
    "scripts": {
        "format": "(npx --no-install prettier --version || (echo '\nPlease install `prettier` via `npm install` first' >&2 && exit 1)) && npx --no-install prettier --quote-props=consistent --write './**/*.{yml,ts}'",
        "test": "vitest --globals=true"
    }
}
